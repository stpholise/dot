import { useReactTable } from '@tanstack/react-table'

const RemittanceTable = () => {

    const table = useReactTable()

  return (
    <div>RemittanceTable</div>
  )
}

export default RemittanceTable